// Two parallel stage<1> functions — both pure (no module-level global writes).
// Same-stage functions are implicitly parallel.

namespace app {
  public:
    void run();
    void transform();
    void validate();

    fn run {
        stage<1> transform();
        stage<1> validate();
    }
}
