// Purity violation: parallel stage<1> function writes to a module-level
// mutable binding (`let counter`), violating the requirement that parallel
// stages operate on isolated state.

namespace app {
  public:
    void run();
    void compute();
    void render();

    fn run {
        stage<1> compute();
        stage<1> render();
    }
}
