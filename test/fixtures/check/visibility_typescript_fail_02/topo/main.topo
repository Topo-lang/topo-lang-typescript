// Visibility violation: private function in `app` called from a different
// namespace `other`, accessed via an `as` alias on a destructured import.

namespace app {
  public:
    void run();
    void compute();

  private:
    void helper();
}

namespace other {
  public:
    void invoke();
}
